{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Huidig PowerShell Script",
            "type": "PowerShell",
            "request": "launch",
            "script": "${file}",
            "cwd": "${fileDirname}"
        }
    ]
}
